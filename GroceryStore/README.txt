#README

# install all the required frameworks and libraries using:

run "pip install -r requirements.txt"

#then, since all the controllers and database schemas along with app and database configurations are present in app.py, therefore,

run "python Project_code/app.py"

##Also all the templates and static folder are in the root directory of "Project_code"

# {{ Manager credentials:  username-manager1  password-pass1  }}
